<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CampoMinado" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CampoMinado" parent="Base.Theme.CampoMinado" />

    <style name="Theme.CampoMinado.Dialog" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowBackground">@color/bg_card</item>
        <item name="colorSurface">@color/bg_card</item>
        <item name="android:textColorPrimary">@color/text_white</item>
    </style>
</resources>
